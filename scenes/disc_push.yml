base: { X: [0, 0, .5], motors, multibody: true, multibody_gravity: false }
fake(base): {}
joint1(fake): { joint: transX, mass: .01, limits: [-.6, .6], sampleUniform: 1. }
joint2(joint1): { joint: transY, mass: .01, limits: [-.6, .6], sampleUniform: 1. }
fing(joint2): { Q: [0, 0, -.33], shape: cylinder, size: [.03, .04], color: [.0, 1., 1.], contact: 1, mass: .1 }

table: { X: [0, 0, .1], shape: box, size: [1.15, 1.15, .1], color: [.6], contact: 1 } #15% bigger footprint than double_sphere's table, same height

obj_base: {}
# the object is a disc (cylinder) free joint: unconstrained physically (a MuJoCo freejoint
# has no limits regardless of the `limits` field below); those limits only bound where
# random starts/goals get sampled from (kept tight in z since it should rest on the table).
obj(obj_base): { joint: free, limits: [-.4,-.4,.165,-1.,-.01,-.01,-1.,.4,.4,.175,1.,.01,.01,1.], Q: [0, 0, .17], shape: cylinder, size: [.04, .13], color: [1., .5, .0], mass: .2, sampleUniform: 1., contact: 1 }

obj_goal(obj_base): { shape: cylinder, size: [.04, .13], color: [.0, 1., .5, .4], simulation: false }

camera_init: { X: [-0.25, 1.6, 0.85, 0.052, -0.066, 0.785, -0.614], focalLength: 1., width: 800, height: 600 }
